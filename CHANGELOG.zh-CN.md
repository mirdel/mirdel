# 更新日志

所有面向用户的重要变化都应记录在此文件中。

本项目使用简单的版本化更新日志格式。每个发布版本应与
`packages/desktop/package.json` 中的桌面端版本一致。

## 0.1.0 - 2026-05-15

### 新增

- 新增 macOS 和 Windows 桌面端构建的初始发布流程。

### 变更

- 桌面端发布资产改为从标签构建发布。

### 修复

- 稳定了 macOS arm64、macOS x64 和 Windows x64 的 CI 打包流程。
