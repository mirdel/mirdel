---
alwaysApply: true
---
* 这是一个 electron 桌面端应用；
* 完全不需要考虑浏览器兼容性；
* 当前依然是开发阶段，完全不要考虑版本兼容；
* 使用 pnpm + monorepo 架构；
* vue 单文件中，请按照 template, script, style 的顺序组织各部分；
* 避免使用 class 来组织脚本，使用模块化函数；
* 尽量使用 @nuxt/ui 组件库，已有mcp工具可使用；
* 使用 @nuxt/ui 组件库时，组件的用法必须完全依据官方文档和mcp为依据，而不是依赖自己的经验或猜测；
* vue 组件尽量使用 tailwindcss 来设置样式，但不绝对；
* icon 尽量使用 lucide 系列图标，除非实在没有合适的；
* 任何时候，都不要自动执行 pnpm dev 启动服务，我不需要；
* 请不要擅作主张或者靠猜测，有任何不清楚的或者不确定的应该主动向我确认；
* 除非非常简单的代码修改任务，否则，必须我明确回复开始改，你才能开始修改代码。如果我没有让你改，你只能输出：方案 + 待确认问题。
* 空数据时，尽量使用 UEmpty 组件；
* 禁止使用 window.alert, window.prompt 等方法，必须用组件；
* 边框应统一使用 `border-default`；
* 对于只有 icon 但没有文字的按钮，一般都需要加上 UTooltip 来辅助说明；